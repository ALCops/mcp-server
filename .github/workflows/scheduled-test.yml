name: Scheduled Compatibility Test

on:
  schedule:
    - cron: "0 6 * * 1" # Every Monday at 06:00 UTC
  workflow_dispatch:

permissions:
  contents: read
  checks: write

jobs:
  test:
    uses: ./.github/workflows/build-test.yml

